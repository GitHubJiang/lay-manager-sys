<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.lay.shop.modules.sys.dao.PriUrlDao">
	<resultMap id="priUrlResultExt" type="com.lay.shop.modules.sys.model.PriUrl">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="priUrlColumnsExt">
	    <![CDATA[
	        	id as id,
	        	acl as acl,
	        	url_id as urlId
	    ]]>
	</sql>
	
	<sql id="priUrlDynamicWhereExt">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>
	       
				<if test="acl!=null">
					and acl = #{acl}
				</if>
	       
				<if test="urlId!=null">
					and url_id = #{urlId}
				</if>
		</where>
	</sql>
	
</mapper>
