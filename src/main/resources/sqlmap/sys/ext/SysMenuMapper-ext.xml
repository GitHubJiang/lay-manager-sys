<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.lay.shop.modules.sys.dao.SysMenuDao">
	<resultMap id="SysMenuResultExt" type="com.lay.shop.modules.sys.model.SysMenu">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="sysMenuColumnsExt">
	    <![CDATA[
	        	id as id,
	        	name as name,
	        	acl as acl,
	        	url as url,
	        	sort_no as sortNo,
	        	parent_id as parentId
	    ]]>
	</sql>
	
	<sql id="sysMenuDynamicWhereExt">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>
	       
				<if test="name!=null">
					and name = #{name}
				</if>
	       
				<if test="acl!=null">
					and acl = #{acl}
				</if>
	       
				<if test="url!=null">
					and url = #{url}
				</if>
	       
				<if test="sortNo!=null">
					and sort_no = #{sortNo}
				</if>
	       
				<if test="parentId!=null">
					and parent_id = #{parentId}
				</if>
		</where>
	</sql>
	
</mapper>
