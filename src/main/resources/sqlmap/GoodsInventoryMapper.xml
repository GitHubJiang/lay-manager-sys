<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">


<mapper namespace="com.baozun.shopdog.primservice.dao.auth.GoodsInventoryDao">
	<resultMap id="GoodsInventoryResult" type="com.baozun.shopdog.primservice.model.auth.GoodsInventory">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="goodsInventoryColumns">
	    <![CDATA[
	        	id as id,
	        	company_code as companyCode,
	        	sku_code as skuCode,
	        	quantity as quantity,
	        	create_time as createTime,
	        	version as version
	    ]]>
	</sql>
	
		<!-- 如果有自增字段 -->
	
			<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
			<insert id="insert" parameterType="com.baozun.shopdog.primservice.model.auth.GoodsInventory" 
				useGeneratedKeys="true" keyProperty="id" 
			 flushCache="true">
		    <![CDATA[
		        INSERT INTO
		        goods_inventory (
		        	company_code ,
		        	sku_code ,
		        	quantity ,
		        	create_time ,
		        	version 
		        ) VALUES (
		        	#{companyCode,javaType=String,jdbcType=VARCHAR} ,
		        	#{skuCode,javaType=String,jdbcType=VARCHAR} ,
		        	#{quantity,javaType=Integer,jdbcType=INTEGER} ,
		        	#{createTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        	#{version,javaType=java.util.Date,jdbcType=TIMESTAMP} 
		        )
		    ]]>
	
			</insert>
		
		<update id="update" parameterType="com.baozun.shopdog.primservice.model.auth.GoodsInventory">
	    <![CDATA[
	        UPDATE goods_inventory SET
		        company_code = #{companyCode,javaType=String,jdbcType=VARCHAR} ,
		        sku_code = #{skuCode,javaType=String,jdbcType=VARCHAR} ,
		        quantity = #{quantity,javaType=Integer,jdbcType=INTEGER} ,
		        create_time = #{createTime,javaType=java.util.Date,jdbcType=TIMESTAMP} ,
		        version = #{version,javaType=java.util.Date,jdbcType=TIMESTAMP} 
	        WHERE 
		        id = #{id} 
	    ]]>
		</update>
	   <delete id="delete" parameterType="java.lang.Long">
	    <![CDATA[
	        delete from goods_inventory where
		        id = #{id} 
	    ]]>
	    </delete> 
	    
	     <delete id="deleteByIds" parameterType="list">
	    
	        delete from goods_inventory 
	        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>	
	  
	    </delete>  
	         
	    <select id="findById" parameterType="java.lang.Long" resultMap="GoodsInventoryResult" flushCache="false">
			select <include refid="goodsInventoryColumns" />
		    <![CDATA[
			    from goods_inventory 
		        where 
		        id = #{id} 
		    ]]>
		</select>	
		<select id="getByIds" parameterType="list"  resultMap="GoodsInventoryResult" flushCache="false">
			select <include refid="goodsInventoryColumns" />
			    from goods_inventory 
		        where id in
				<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
#{item}				</foreach>					
		</select>	
	<sql id="goodsInventoryDynamicWhere">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>
	       
				<if test="id!=null">
					and id = #{id}
				</if>
	       
				<if test="companyCode!=null">
					and company_code = #{companyCode}
				</if>
	       
				<if test="skuCode!=null">
					and sku_code = #{skuCode}
				</if>
	       
				<if test="quantity!=null">
					and quantity = #{quantity}
				</if>
	       
				<if test="createTime!=null">
					and create_time = #{createTime}
				</if>
	       
				<if test="version!=null">
					and version = #{version}
				</if>
		</where>
	</sql>
	

	<select id="getCount" resultType="long">
        select count(*) from goods_inventory 
    </select>
    <select id="findListCountByQueryMap" resultType="long">
        select count(*) from goods_inventory 
		<include refid="goodsInventoryDynamicWhere"/>    
    </select>
    
    <select id="findListCountByParam" resultType="long">
        select count(*) from goods_inventory 
		<include refid="goodsInventoryDynamicWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findListByQueryMapWithPage" resultMap="GoodsInventoryResult">
    	select <include refid="goodsInventoryColumns" />
	    from goods_inventory 
		<include refid="goodsInventoryDynamicWhere"/>
		
    </select>
    
    <select id="findListByQueryMap" resultMap="GoodsInventoryResult">
    	select <include refid="goodsInventoryColumns" />
	    from goods_inventory 
		<include refid="goodsInventoryDynamicWhere"/>
		
    </select>
    
    <select id="findListByParam" resultMap="GoodsInventoryResult">
    	select <include refid="goodsInventoryColumns" />
	    from goods_inventory 
		<include refid="goodsInventoryDynamicWhere"/>
		
    </select>
</mapper>
